name: OSS Laravel candidate baseline
on:
  push:
    branches:
      - automation/laravel-validation-r12
permissions:
  contents: read
jobs:
  test:
    if: github.repository == 'dijedontahiri/framework'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          ref: af99aa382c44dbb13cea40ab39bc77c24e0a6bb4
          persist-credentials: false
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
          tools: composer:v2
      - run: composer install --no-interaction --no-progress --prefer-dist
      - run: vendor/bin/phpunit tests/Support/SupportLazyCollectionMapInternalCallableTest.php
